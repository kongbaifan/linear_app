# Linage 设计规范

所有视觉决策以本文件为准。任何新组件先查这里,再写 CSS;不在规范内的值不允许硬编码。

## 1. 设计原则

1. **内容优先** — 界面为任务和 diff 服务,装饰性元素一律从简。
2. **两个角色** — 全站只有"我"(K 头像,accent 底)和 Agent(L 字标,深色圆底),不出现第三种头像形态。
3. **状态诚实** — 模拟结果必须带标注;失败必须可见;加载必须有 spinner。
4. **键盘可达** — 一切可点的东西必须可 Tab 聚焦,`:focus-visible` 显示 accent 焦点环。

## 2. Design Tokens(`global.css :root`)

### 间距(4 点网格)

`--s1: 4px` `--s2: 8px` `--s3: 12px` `--s4: 16px` `--s5: 20px` `--s6: 24px`

规则:组件内 padding 用 s2–s4;区块之间 s4–s6;禁止 5/7/9/11 等散值。

### 字号

| token | 值 | 用途 |
|---|---|---|
| `--fz-xs` | 11px | 徽章、次级计量(时间、行号) |
| `--fz-sm` | 12px | 辅助文本、标签、菜单 |
| `--fz-md` | 13px | 正文、按钮、列表标题(默认) |
| `--fz-lg` | 15px | 区块标题 |
| `--fz-xl` | 20px | 页面主标题(issue 标题) |

字重只用三档:400(正文)、550(强调/行标题)、600(标题)。中文行高 ≥1.6,英文 1.5。

### 圆角

`--radius-sm: 6px`(输入框、菜单项)· `--radius: 8px`(卡片、按钮容器)· `--radius-lg: 10px`(菜单、diff 卡)· `--radius-xl: 12px`(弹窗、设置区块)

### 阴影(仅浮层可用)

`--shadow-menu`(下拉/用户菜单)· `--shadow-modal`(弹窗/命令面板)。卡片一律用边框分层,不用阴影。亮色主题的阴影值在 `[data-theme='light']` 中覆盖。

### 动效

`--dur: 120ms`(默认)· `--dur-slow: 200ms`(面板滑入)· `--ease: cubic-bezier(0.2, 0.9, 0.3, 1)`。
只对 opacity/transform/background/border-color 做过渡;浮层入场统一 `pop-in`。禁止超过 250ms 的 UI 动效。

### 控件高度

`--ctl: 30px`(默认按钮/输入)· `--ctl-sm: 26px`(行内小按钮)· 输入框 32px · 图标按钮 28×28 · 列表行 40px · 面板头 48px。

## 3. 颜色语义

颜色只能通过语义变量使用,禁止裸写 hex(图表/标签色除外):

- 背景层级:`--bg-app`(最底)→ `--bg-surface`(主面板)→ `--bg-raised`(卡片)→ `--bg-float`(浮层)
- 交互态:`--bg-hover` → `--bg-active`(选中/按下)
- 文本层级:`--text-1`(主)→ `--text-2`(次)→ `--text-3`(辅助/图标)
- 状态色:`--yellow` 进行中 · `--green` 完成/成功 · `--red` 失败/危险 · `--accent-text` 链接/待审查
- 新增语义色必须同时提供亮色主题覆盖。

## 4. 组件规则

- **按钮**:默认描边款;每个视图最多一个 `primary`;危险操作 `danger`(红字红边,不填充);禁用态 opacity .45。
- **菜单/下拉**:radius-lg + shadow-menu,菜单项高 30px,分隔线 `menu-divider`,选中项右侧 ✓。
- **空状态**:居中,虚线圆图形 + 一句说明 + 下一步动作,行高 1.7,宽度 ≤380px。
- **徽章**:`sample-badge`(示例)、`model-badge`(模型/仓库)统一小写字号、bg-active 底、无边框优先。
- **列表行**:hover 背景过渡 120ms;选中用 `--bg-active`;拖拽指示用 accent 内阴影线。

## 5. 文案

- 中文为默认语言;中文文案一律使用全角标点(,。:「」),禁止半角逗号夹在汉字之间。
- 中英文与数字之间保留一个空格("委派给 Agent"、"3 个事项")。
- 所有面向用户的字符串必须走 `i18n.tsx`,两种语言同时补齐(MessageKey 类型会在编译期强制)。

## 6. 可达性

- 全局 `:focus-visible` accent 焦点环,禁止 `outline: none` 不给替代。
- 图标按钮必须有 `title`;纯色徽章需同时有文字。
- 对比度:text-2 于 bg-surface ≥ 4.5:1;两个主题都要检查。
